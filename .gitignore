.venv/
node_modules/
__pycache__/
*.pyc
*.tmp
logs/*.log
.DS_Store

# Large REGENERABLE raw snapshots — git-ignored to keep the repo manageable.
# Provenance is preserved: every one has a SHA-256 + URL + retrieved_at in
# data/manifest.csv, and `make fetch` regenerates them. The irreplaceable Nepal DCR
# PDF and the OECD/ADB anchor snapshots are kept in git.
data/raw/us_fa/
data/raw/oecd_crs/
data/raw/iati_dportal/
data/raw/aiddata_gcdf/
# redundant with core_long.csv (regenerated by `make build`)
data/processed/core_long.json
